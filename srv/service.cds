service main {
    entity Order    {
        key id      : UUID;
            order   : String;
    }

    entity Order2   {
        key id      : UUID;
            order   : String;
    }

    entity Order3   {
        key id      : UUID;
            order   : String;
    }
    entity Order4   {
        key id      : UUID;
            order   : String;
    }
}